# Ejercicio 1: Detección de Enemigos

Recorre una lista de distancias y usa un condicional `if/else` para imprimir `"Peligro"` si la distancia es menor a 50, o `"A salvo"` si es 50 o mayor.
```python
# Tu tarea:
distancias = [120, 45, 8, 300]
# Escribe aquí tu bucle y condicionales...
```

> **Ejemplo Guía:** (Saber si un coche va muy rápido; más de 100km/h es "Multa", sino "Correcto")
> ```python
> velocidades = [80, 120, 90, 140]
> for v in velocidades:
>     if v > 100:
>         print("Multa")
>     else:
>         print("Correcto")
> ```
